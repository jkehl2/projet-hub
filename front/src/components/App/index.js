// == Import npm
import React from 'react';
import PropTypes from 'prop-types';

// == IMPORTS COMPOSANTS

// == IMPORTS CONTAINERS

// == STYLES
import './app.scss';

// == Composant
const App = ({}) => (
  <div className="app" />
);

App.propTypes = {
};

// == Export
export default App;
