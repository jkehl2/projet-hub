// == Import npm
import React from 'react';
import PropTypes from 'prop-types';

// == IMPORTS COMPOSANTS

// == IMPORTS CONTAINERS

// == STYLES
import './map.scss';

// == Composant
const Map = ({ props }) => (
  <div className="app" />
);

Map.propTypes = {
  props: PropTypes.object.isRequired,
};

// == Export
export default Map;
