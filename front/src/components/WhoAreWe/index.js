// == Import npm
import React from 'react';
import PropTypes from 'prop-types';

// == IMPORTS COMPOSANTS

// == IMPORTS CONTAINERS

// == STYLES
import './app.scss';

// == Composant
const App = ({ props }) => {
  return (
    <div className="app">
    </div>
  );
};

App.propTypes = {
  props: PropTypes.object.isRequired,
};

// == Export
export default
