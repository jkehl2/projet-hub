// == Import npm
import React from 'react';
import PropTypes from 'prop-types';

// == IMPORTS COMPOSANTS

// == IMPORTS CONTAINERS

// == STYLES
import './home.scss';

// == Composant
const Home = ({ props }) => (
  <div className="home" />
);

Home.propTypes = {
  props: PropTypes.object.isRequired,
};

// == Export
export default Home;
